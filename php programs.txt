Use PHP write the following programs using (branching , Looping)
1. to print grades of a students (O,E,A,B,C,D,F, Wront Entry)
2. switch(v) {

   case const1:
   statements;
   break;
   case const2:
    statements;
    break;
    ......
    default:
    }
print the color of a rainbow 
VIBGYOR
switch(c) {
case 'v':
case 'V':
echo 'violet color';
break;
case 'I':
case 'i':
echo 'Indigo Color';
case 'B':
case 'b':
echo 'Blue Color';
break;
default:
echo 'other color';
}


 Looping while, dowhile, for , foreach
3. print sum of digit of a number
save the file as loop1.php
<html>
<head>
</head>
<body>
  <?php
   $n=(int)readline('Enter an integer');
   $rem=0;
   $sum=0;
    while($n!=0) {
      $rem=$n%10;
      $sum=$sum+$rem;
      $n=$n/10;
      
    }
  echo 'Sum of digit of $n is';
  echo '<br>';
  echo '$sum;   
  ?>
</body></html>




4. print no. of digits of a number
<body>
  <?php
   $n=(int)readline('Enter an integer');
   $rem=0;
   $count=0;
    while($n!=0) {
      $rem=$n%10;
      $count++;
      $n=$n/10;
      
    }
  echo 'count of digit of $n is';
  echo '<br>';
  echo '$count;   
  ?>


5. print reverse of a number
<?php
   $n=(int)readline('Enter an integer');
   $rem=0;
   $reverse=0;
     do {
      $rem=$n%10;
      $reverse=$reverse*10+$rem;
      $n=$n/10;      
    }while($n!=0);
  echo 'reverse of $n is';
  echo '<br>';
  echo '$reverse;   
  ?>

4. find sum of 1/2*3 + 2/3*4 + 3/4*5 .....n/(n+1)*(n+2)
save the file as loop4.php
<html>
<head>
<title> loop for series sum </title>
</head>
<body>
<?php
$i=1;
$sum=0.0;
$n=(int)readline('Enter an integer');
for($i=1;$i<=$n;$i++) {
$sum=$sum+float($i)/(($i+1)*($i+2));
}
echo 'Sum of series is';
echo '<br>';
echo '$sum';
?>
</body></html>
5. print factorial of a number
<html>
<head>
<title> loop for factorial </title>
</head>
<body>
<?php
$i=1;
$fact=1;
$n=(int)readline('Enter an integer');
for($i=1;$i<=$n;$i++) {
$fact=$fact*$i;
}
echo 'Factorial is';
echo '<br>';
echo '$fact';
?>
</body></html>
7. check no. is perfect of not


8. check a number is prime or not.





9. check a number is palindrome or not
10. Reverse a string  
11.	Write a PHP program to read 5 student marks, store them in 
an array, and compute:

i)	total
ii)	average
iii)	highest mark
iv)	lowest mark
Use appropriate loops (for / foreach) and justify your loop selection.
<?php
$marks=array(10,20,23,12,25);
$total=0;
for ($i=0;$i<=4;$i++) {
$total=$total+$marks[i];
}
foreach($marks as $i) {
$total=$total+$i;
}
$avg=$total/5.0;
$highest=$lowest=$marks[0];
foreach ($marks as $i) {
if($i>$highest) {
$highest=$i;
}
if($i<$lowest) {
$lowest=$i;
}
}
echo 'total is $total';

echo 'average is $average';
echo 'highest is $highest';
echo 'lowest is $lowest';

?>







